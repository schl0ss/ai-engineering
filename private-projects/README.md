# Private Projects

This directory is intentionally local-only.

Use it for private project context, client-specific notes, production deploy details, private prompts, internal eval sets, credentials references, and anything that should not be published.

Only this README and `.gitignore` should be committed.
